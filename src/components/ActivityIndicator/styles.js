import { StyleSheet } from 'react-native';
import { constants } from '@constants';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    top: constants.dimensionsWindow.height / 2,
    alignSelf: 'center',
  },
});
